// The soundscape browser contract owns one Edge/OOPIF fixture so the health,
// canvas, persistence, and input assertions observe the same opaque child.
// Importing the contract runs its real-browser probe and propagates its exit
// status; the interaction-specific assertions are reported as
// sandboxLyricGestures, sandboxBlankDoubleClickReset, and
// sandboxPlayerInteractions.
await import('./check-sonic-topography-wallpaper-engine-preset.mjs');
