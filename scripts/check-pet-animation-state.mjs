// Compatibility entry point retained for existing QA commands. The desktop
// mascot is now the white topology particle orb, so its authoritative contract
// lives in the dedicated particle-orb check.
await import('./check-pet-particle-orb.mjs');
